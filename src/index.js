import React from 'react';
import ReactDOM from 'react-dom';
import ShoppingList from './screens/shopping-screen'

ReactDOM.render(
  <React.StrictMode>
    <ShoppingList />
  </React.StrictMode>,
  document.getElementById('root')
);
